# Since no convention has been created yet, I have taken some liberties on how it is formated.  The file has been self assembled

L1:	mov r1, 1   # load value 1 into r1
	mov r2, 2   # load value 2 into r2 
	mov r1, r3  # move value (1) of r1 into r3
	add r2, r3  # add r2 and r3, store into r3.